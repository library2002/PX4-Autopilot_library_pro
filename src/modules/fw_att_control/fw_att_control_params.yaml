module_name: fw_att_control
parameters:
- group: FW Attitude Control
  definitions:
    FW_R_TC:
      description:
        short: Attitude Roll Time Constant
        long: |-
          This defines the latency between a roll step input and the achieved setpoint
          (inverse to a P gain). Smaller systems may require smaller values.
      type: float
      default: 0.4
      unit: s
      min: 0.2
      max: 1.0
      decimal: 2
      increment: 0.05
    FW_P_TC:
      description:
        short: Attitude pitch time constant
        long: |-
          This defines the latency between a pitch step input and the achieved setpoint
          (inverse to a P gain). Smaller systems may require smaller values.
      type: float
      default: 0.4
      unit: s
      min: 0.2
      max: 1.0
      decimal: 2
      increment: 0.05
    FW_P_RMAX_POS:
      description:
        short: Maximum positive / up pitch rate setpoint
      type: float
      default: 60.0
      unit: deg/s
      min: 0.0
      max: 180
      decimal: 1
      increment: 0.5
    FW_P_RMAX_NEG:
      description:
        short: Maximum negative / down pitch rate setpoint
      type: float
      default: 60.0
      unit: deg/s
      min: 0.0
      max: 180
      decimal: 1
      increment: 0.5
    FW_R_RMAX:
      description:
        short: Maximum roll rate setpoint
      type: float
      default: 70.0
      unit: deg/s
      min: 0.0
      max: 180
      decimal: 1
      increment: 0.5
    FW_Y_RMAX:
      description:
        short: Maximum yaw rate setpoint
      type: float
      default: 50.0
      unit: deg/s
      min: 0.0
      max: 180
      decimal: 1
      increment: 0.5
    FW_W_EN:
      description:
        short: Enable wheel steering controller
        long: |-
          Only enabled during automatic runway takeoff and landing.
          In all manual modes the wheel is directly controlled with yaw stick.
      type: boolean
      default: 0
    FW_WR_P:
      description:
        short: Wheel steering rate proportional gain
        long: |-
          This defines how much the wheel steering input will be commanded depending on the
          current body angular rate error.
      type: float
      default: 0.5
      unit: '%/rad/s'
      min: 0.0
      max: 10
      decimal: 3
      increment: 0.005
    FW_WR_I:
      description:
        short: Wheel steering rate integrator gain
        long: |-
          This gain defines how much control response will result out of a steady
          state error. It trims any constant error.
      type: float
      default: 0.1
      unit: '%/rad'
      min: 0.0
      max: 10
      decimal: 3
      increment: 0.005
    FW_WR_IMAX:
      description:
        short: Wheel steering rate integrator limit
      type: float
      default: 0.4
      min: 0.0
      max: 1.0
      decimal: 2
      increment: 0.05
    FW_W_RMAX:
      description:
        short: Maximum wheel steering rate
        long: |-
          This limits the maximum wheel steering rate the controller will output (in degrees per
          second).
      type: float
      default: 30.0
      unit: deg/s
      min: 0.0
      max: 90.0
      decimal: 1
      increment: 0.5
    FW_WR_FF:
      description:
        short: Wheel steering rate feed forward
      type: float
      default: 0.2
      unit: '%/rad/s'
      min: 0.0
      max: 10
      decimal: 2
      increment: 0.05
    FW_PSP_OFF:
      description:
        short: Pitch setpoint offset (pitch at level flight)
        long: |-
          An airframe specific offset of the pitch setpoint in degrees, the value is
          added to the pitch setpoint and should correspond to the pitch at
          typical cruise speed of the airframe.
      type: float
      default: 0.0
      unit: deg
      min: -90.0
      max: 90.0
      decimal: 1
      increment: 0.5
    FW_MAN_YR_MAX:
      description:
        short: Maximum manually added yaw rate
        long: |-
          This is the maximally added yaw rate setpoint from the yaw stick in any attitude controlled flight mode.
          It is added to the yaw rate setpoint generated by the controller for turn coordination.
      type: float
      default: 30.0
      unit: deg/s
      min: 0
      decimal: 1
      increment: 0.5
    FW_MAN_R_MAX:
      description:
        short: Maximum manual roll angle
        long: Applies to both directions in all manual modes with attitude stabilization
      type: float
      default: 45.0
      unit: deg
      min: 0.0
      max: 90.0
      decimal: 1
      increment: 0.5
    FW_MAN_P_MAX:
      description:
        short: Maximum manual pitch angle
        long: Applies to both directions in all manual modes with attitude stabilization
          but without altitude control
      type: float
      default: 30.0
      unit: deg
      min: 0.0
      max: 90.0
      decimal: 1
      increment: 0.5
    FW_FORM_YAW_EN:
      description:
        short: Accept yaw rate command from the chain-wing formation link
        long: |-
          When enabled, the yaw rate command received on
          vehicle_attitude_setpoint.yaw_sp_move_rate is added to the yaw rate
          setpoint, in the same way the pilot yaw stick is added in manual modes.
          This gives the wingtip followers of a chain-wing aircraft a yaw channel:
          the fixed-wing attitude loop strips the yaw error (an aircraft has no
          direct yaw authority through attitude), so the yaw rate setpoint is the
          only place a yaw command can enter. The followers' own turn
          coordination feedforward is kept, so only the shared intent is added.
          Leave disabled unless this vehicle is a chain-wing follower.
      type: boolean
      default: 0
